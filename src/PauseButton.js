import React from 'react';

class PauseButton extends React.Component {
  // constructor(props){
  //   super(props);
  // }

  render(){
    return(
      <button>Pause</button>
    );
  }
};

export default PauseButton;
